{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsc -b && node dist/index.js",
    "dev:nodemon": "nodemon --watch 'src/**/*.ts' --exec 'tsc -b && node dist/index.js'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@types/express": "^5.0.6",
    "express": "^5.2.1",
    "nodemon": "^3.1.14",
    "redis": "^5.11.0",
    "typescript": "^6.0.2"
  }
}
